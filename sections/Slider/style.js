import styled from "styled-components";

export const SliderContent = styled.div`
  position: relative;
  img {
    width: 100%;
  }
  .banner_content {
    position: absolute;
    top: 0;
    left: 0;
    transform: translate(50%, 50%);
  }
`;
